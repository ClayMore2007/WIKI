# Local Apps

本目录用于存放本地 Web App。

## Apps

- `stock/`：股票产业链本地 Web App。

## Agent 入口

用户说“打开股票”“查看股票”“打开股票 app”“查看股票 App”“查看股票应用”等类似指令时，默认进入 `stock/`，按 `stock/README.md` 启动并打开股票产业链本地 Web App。

默认使用系统默认浏览器；只有用户明确要求在 Codex app 内查看时，才使用 Codex 内置浏览器。

快捷入口：

```powershell
.\open-stock.ps1
```
